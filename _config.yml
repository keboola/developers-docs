defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "main"

exclude:
  - docker-compose.yml
  - Dockerfile
  - Gemfile
  - Gemfile.lock
  - README.md
  - CNAME

sass:
  style: compressed

gems:
  - jekyll-sitemap
